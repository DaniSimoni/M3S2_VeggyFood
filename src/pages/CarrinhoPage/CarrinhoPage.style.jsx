import styled from 'styled-components'
import { COLORS } from '../../utils/styles/style'

export const ContainerCheckout = styled.div`
    width: 70vw;
    height: 70vh;
    margin-top: 3rem;
    background-color: transparent;
    box-shadow: .3rem .3rem 1rem ${COLORS.$darkGreen};

`